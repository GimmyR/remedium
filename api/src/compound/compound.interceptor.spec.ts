import { CompoundInterceptor } from './compound.interceptor';

describe('CompoundInterceptor', () => {
  it('should be defined', () => {
    expect(new CompoundInterceptor()).toBeDefined();
  });
});
